# %% [markdown]
# # Steady polar example (placeholder)
#
# This example will run a steady angle-of-attack polar end to end:
# define a campaign, generate version-validated FlightStream scripts,
# execute the solver headless, and assemble the sweep into a table.
#
# It becomes runnable at milestone M2, when the script builder, executor,
# and loads parser land. It is committed at M0 so the examples folder and
# the percent-format policy (no notebooks in Git) exist from day one.

# %%
"""Placeholder for the steady polar example, runnable at milestone M2."""

import pyflightstream

print(f"pyflightstream {pyflightstream.__version__}: example arrives at milestone M2")
