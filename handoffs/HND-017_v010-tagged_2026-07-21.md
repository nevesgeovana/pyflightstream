# HND-017: v0.1.0 tagged (2026-07-21)

## 1. Context

Same-session continuation after HND-016, on Geovana's instruction to
push and run the next step. Pushed the M5 session (2c8fb0d..b277334,
CI run 29869650235 green), then cut v0.1.0 through the release skill,
private phase.

## 2. Release checks (release skill steps 1-5)

1. Definition of done: 179 tier 1 tests green locally and in CI;
   command entries schema-enforced (manual_ref, phase, layout, typed
   args); parsers with fixtures since M2; numpydoc via ruff D rules;
   docs build strict; solver-evidence changes carry committed reports;
   pending probes tracked (PLN-012, import trio,
   SET_ANALYSIS_SYMMETRY_LOADS, 26.100 backfill at v0.2+).
2. Evidence currency for the flagship 26.120 (build 7012026): compat
   CMP-26120_2026-07-21_full and physics PHY-26120_2026-07-21_full,
   the capstone drift matrix, and the SMI-01 triage pair, all dated
   2026-07-21.
3. Version bump 0.0.1.dev0 to 0.1.0 in pyproject.toml (SemVer,
   decoupled from FlightStream versions).
4. CHANGELOG.md created (Keep a Changelog): the M0..M5 content plus
   the per-version evidence summary.
5. sdist and wheel built clean: 24 packaged data YAMLs (commands,
   _meta, physics references), no pdf/ipynb/_private content in the
   wheel; dist/ and site/ gitignored. Annotated tag v0.1.0 pushed;
   release commit 38c091c green in CI (run 29869821677).

Step 6 (invariants audit, PyPI upload) belongs to the public phase at
v0.2+ per the milestone map and was not run.

## 3. Changes persisted

* pyproject.toml version 0.1.0; CHANGELOG.md; tag v0.1.0 (pushed).
* STATUS.md v0.1.0 Done; logbook row; this handoff.

## 4. Open questions and contradictions

Carried unchanged from HND-016: ProperDocs fork decision (Geovana,
now relevant for the v0.2+ public phase rather than this tag);
PLN-012; xarray gate; SMI genericization; SWEEPER pass;
getting-started and campaign tutorial pages; 26.000/26.100 backfill
probing.

## 5. Single highest-value next action

Open the v0.2+ line: pick between the public-release track (remaining
PHY cases, backfill probing, invariants audit, PyPI) and the
declarative matrix successor; the ProperDocs decision gates the docs
toolchain either way.
