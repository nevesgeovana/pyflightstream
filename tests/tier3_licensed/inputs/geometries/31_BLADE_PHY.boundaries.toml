# Boundary inventory of 31_BLADE_PHY.fsm, read from its mesh block by `pyfs-matrix inventory`.
# The solver's own order: the name at position i is boundary i (1-based).
file = "31_BLADE_PHY.fsm"
boundaries = [
    "Blade1",
]
