# Boundary inventory of 40_PUSHER.fsm, read from its mesh block by `pyfs-matrix inventory`.
# The solver's own order: the name at position i is boundary i (1-based).
file = "40_PUSHER.fsm"
boundaries = [
    "Body",
    "Base",
    "Blade1",
]
