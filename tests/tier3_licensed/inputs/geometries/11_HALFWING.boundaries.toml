# Boundary inventory of 11_HALFWING.fsm, read from its mesh block by `pyfs-matrix inventory`.
# The solver's own order: the name at position i is boundary i (1-based).
file = "11_HALFWING.fsm"
boundaries = [
    "Wing",
]
