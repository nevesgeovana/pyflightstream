# Boundary inventory of 30_BLADE.fsm, read from its mesh block by `pyfs-matrix inventory`.
# The solver's own order: the name at position i is boundary i (1-based).
file = "30_BLADE.fsm"
boundaries = [
    "Blade1",
]
