# Boundary inventory of 20_BODY.fsm, read from its mesh block by `pyfs-matrix inventory`.
# The solver's own order: the name at position i is boundary i (1-based).
file = "20_BODY.fsm"
boundaries = [
    "Body",
    "Base",
]
