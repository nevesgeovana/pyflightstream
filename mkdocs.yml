# Docs configuration. Kept at the repository root (mkdocs requires the
# config file outside docs_dir); the DLV-004 tree placed it under docs/,
# a deliberate small deviation recorded at M0.
site_name: pyflightstream
site_description: Version-aware, didactic Python driver for FlightStream
repo_url: https://github.com/nevesgeovana/pyflightstream

theme:
  name: material

nav:
  - Home: index.md
