# Compat report: FlightStream 26.124 (2026-09-25)

This is a compat-format transcription of the licensed probe T20, reported as
RPT-075 (`reports/RPT-075_the-boundary-layer-profile-still-holds-the-script-on-26124_2026-09-25.md`).
No solver was invoked while writing it, and it does not claim a probe-harness
execution. The paired YAML records the `broken` outcome for
`EXPORT_BL_VELOCITY_PROFILE`, from which `pyfs-qa apply-compat` wrote the 26.124 row.

| Run | The one addition, before `EXPORT_LOG` | Result | Profile file | Log export |
|---|---|---|---|---|
| Control | nothing | returned 0 in 9.4 s | | written |
| BL profile | `EXPORT_BL_VELOCITY_PROFILE 1 x y z` and the file on the next line | killed at the 120 s timeout | not written | not written |

Every export before the command was written with the control's loads. The command
holds the script: on 26.122 it opened a modal window that waits for a person
(RPT-027), and on 26.124 the run did not pass it.

What this does not claim: what the profile file holds, and any build other than
26.124.
