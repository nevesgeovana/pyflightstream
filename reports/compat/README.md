# Command compatibility reports

Committed evidence from Tier 2 probe runs on licensed machines: for each
FlightStream version, which database commands are verified, broken, or
unprobed, with pointers to the evidence. Command database statuses are
promoted only from these reports, never hand-edited (CLAUDE.md
invariant 3). First report arrives at milestone M3.
